'use strict';

describe('lyonRewards.ranking module', function() {

  beforeEach(module('lyonRewards.ranking'));

  describe('ranking controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var RankingCtrl = $controller('RankingCtrl');
      expect(RankingCtrl).toBeDefined();
    }));

  });
});