'use strict';

describe('lyonRewards.partners module', function() {

  beforeEach(module('lyonRewards.partners'));

  describe('partners controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PartnersCtrl = $controller('PartnersCtrl');
      expect(PartnersCtrl).toBeDefined();
    }));

  });
});