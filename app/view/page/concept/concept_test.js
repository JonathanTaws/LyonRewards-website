'use strict';

describe('lyonRewards.concept module', function() {

  beforeEach(module('lyonRewards.concept'));

  describe('concept controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ConceptCtrl = $controller('ConceptCtrl');
      expect(ConceptCtrl).toBeDefined();
    }));

  });
});